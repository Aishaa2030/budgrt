import React from "react";

// Placeholder for Power Platform authentication context.
// In production, initialize @microsoft/power-apps SDK here.
export default function PowerProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
